# Keep MapStruct mapper interfaces and implementations in the base classloader
# This prevents them from being cleared on DevTools restart
# MapStruct generates Spring beans, so both interfaces and implementations must stay loaded
restart.exclude.mappers=/lt/creditco/cupa/service/mapper/.*\.class

