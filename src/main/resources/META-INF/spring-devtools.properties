# Keep MapStruct-generated mapper implementations in the base classloader
# This prevents them from being cleared on DevTools restart
# Pattern matches all *Impl.class files in the mapper package
restart.exclude.mappers=/lt/creditco/cupa/service/mapper/.*Impl\.class

